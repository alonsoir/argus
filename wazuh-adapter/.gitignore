# /vagrant es carpeta COMPARTIDA entre VMs, asi que cada una construye en su
# propio directorio con sufijo (build-wazuh, build-defender...). El patron
# tiene que cubrirlos todos, no solo `build/`.
build*/